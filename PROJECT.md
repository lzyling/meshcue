# Agent 通用 3D 模型审阅工作台

> **定位补充（2026-09-09，用户已确认）**：本项目是「Agent 通用 3D 审阅与标注工具」，面向不同 Agent harness；OpenClaw 是首个适配平台，不是唯一归属。软件、适配、MCP 与 Skill 的边界见 [产品定位说明](POSITIONING.md)。现有项目代号／目录不变。

> **2026-09-10 最新决策**：正式交互窗口使用标准浏览器，取消 OpenClaw／Codex 等内置浏览器适配及 Portal 入口待办；保留 Agent 接线和原会话回传。见 [入口决策](BROWSER-ACCESS-DECISION.md)。

| 项目项 | 当前记录 |
|---|---|
| 项目代号 | `3d-agent-review` |
| 发起人／负责人 | Kelven |
| 协助管理 | POP |
| 正式立项 | 2026-09-09，用户在实时语音中明确要求 |
| 阶段 | **0.3 已获基本认可；09-10 在 Telegram Coding 接续，0.4 内网交付与原会话闭环计划已整理，未开始新一轮实现／部署** |
| 已完成 | 本地网页、真实三维标注、草稿与换版保护、同会话桥接、多轮自动化及浏览器验证 |
| 版本管理 | 项目级本地 Git，`main` 分支；未配置远程、未推送 |
| 运行状态 | 0.3 历史交付入口为本机 `http://127.0.0.1:43173/`；09-10 13:32 接续时健康请求未连通，原因待查，不能沿用历史记录声称此刻在线；未重启服务 |
| 未进行 | 通用适配层抽离、MCP／正式 Skill 封装、其他 harness 接入、付费生成、远端发布 |

## 目标

一个本地网页工作台，让用户与原会话中的 Agent 看着同一个 3D 模型，通过编号、彩色区域标记和对话，准确沟通修改位置及要求。适配目标面向不同 harness，当前首先接入 OpenClaw。

**一套审阅界面，兼容不同建模来源：** OpenClaw 写程序生成的参数化模型，以及 OpenClaw 调 Tripo 等工具生成的人偶，都进入同一套展示和反馈流程。

## 已确认的方向

- 保留各 harness 原本的主 Agent，不要求用户转用查看器厂商的 AI；当前实例由 OpenClaw 主控。
- 原会话是唯一对话入口；审阅页只显示模型与标注，在用户自己的标准浏览器直接操作。可将两个窗口并排摆放，不要求同页嵌入；不再适配 harness 内置浏览器。
- 模型采用单一大视窗，只显示 Agent 指定的当前版本；用户已撤回自行切换历史版本的方案，避免版本与标记错配。
- Agent 负责在审阅开始前交付正确最新版本；用户标注期间不得切换模型或丢失未提交工作。版本同步错误由 Agent 跟进，不转嫁给用户。
- 用户可旋转、缩放模型，双击表面落点标签，左键拖动旋转无需切工具；彩色画笔默认只标可见表面，不穿透。0.3 新标签统一用 A/B/C/D 等大写字母，同轮删除不重排、不复用；旧数字标记保留，见需求 R04。
- 点击「反馈给 Agent」，让 OpenClaw 得到实际位置、区域和标注编号并确认收到；缺少修改说明时在原会话主动询问，文字沟通即可，不要求语音，也不凭标记自行猜测修改。
- 参数化源工程保留，不能为了统一展示而丢失可编辑参数。
- 本轮布局要求已在 0.3 实现：缩小顶部标题并压缩上下非必要占位，把可用空间优先留给模型预览；用户手感待实测，见 R27。
- 本轮下载要求已在 0.3 实现：用户直接下载当前正在预览的模型版本，不必再请 Agent 整理文件，也不误用后台新版本；标准 Chrome 原档下载校验通过，见 R28。

## 当前选型状态

**初版采用 Three.js + 轻量标注层 + 本地 Node 服务。** 3D-Annotator 保留为研究参考，不作为运行依赖。OCP 仅作后续 CAD 子链参考。初版 GLB／STL 使用同一审阅流程，原始媒体文件不被标注写回。

0.2 已移除 0.1 的复制聊天框，保留原会话作为唯一入口。历史 Control UI 侧栏开发与用户阶段验收保留在 [侧栏验收](SIDEBAR-ACCEPTANCE-20260909.md)，不再构成未来产品依赖或验收门槛。Portal 入口任务已取消，既有宿主补丁未在本次回滚。能准确标记位置，不等于已支持任意模型自动精准修形。

## 项目文件

- [产品定位补充与跨 harness 边界](POSITIONING.md)
- [标准浏览器入口决策与范围变更](BROWSER-ACCESS-DECISION.md)
- [仓库入口与版本管理约定](README.md)
- [需求与验收方向](REQUIREMENTS.md)
- [阶段和待办](ROADMAP.md)
- [0.4 内网交付与原会话闭环计划](ITERATION-V04-PLAN.md)
- [产品／源码调查与维护复核](RESEARCH-20260909.md)
- [官方参考素材](REFERENCES.md)
- [实施记录](IMPLEMENTATION.md)
- [Agent 操作接口](AGENT-INTERFACE.md)
- [初版验收与限制](ACCEPTANCE-20260909.md)
- [素材投递历史证据](evidence/20260909-media/)

## 管理约定

项目文档、评估、决策与代码统一归本目录。媒体留在 workspace 的 `media/`。2026-09-09 用户明确授权在休息期间多轮迭代做出可测试初版，取代此前只讨论设计的阶段限制；只启用项目本地服务，不改变 Gateway 配置或擅自远端发布。

用户已确认本次 Control UI 图片显示及短片播放正常。这只验收素材回传，不代表建模／标注应用已经完成。
