# 交互入口决策：标准浏览器优先，取消宿主内置浏览器适配

状态：**2026-09-10 Kelven 明确确认，作为后续设计基准。** 本文件取代此前要求在 Control UI／其他 harness 侧栏内直接操作的设计，不改变已发生的开发、测试和部署事实。

## 正式产品入口

- 用户在自己使用的标准浏览器窗口／标签页直接操作工作台，例如 Chrome、Safari、Edge、Firefox。具体支持版本及兼容矩阵待验证，不据此宣称四者均已验收。
- 模型渲染、旋转、缩放、点标签、画笔、橡皮擦、油漆桶、理解回显与下载，直接使用浏览器的网页能力；不经 harness 的远端截图、手势转发或下载中转。
- 原 Agent 会话可以与工作台窗口并排使用，但不是必须嵌入同一网页；工作台仍不复制聊天框。
- 既有 0.3 本机入口是 <http://127.0.0.1:43173/>，仅供运行服务的同一台机器访问；这是现有实现边界，不等于已满足下节的链接交付目标。09-10 13:46 已明确需增加 Windows 访问 MacBook Pro 的内网兼容；当前未开放监听或实施授权，公网访问未纳入已确认范围。

## 从 Telegram 交付可用预览链接（2026-09-10 13:40）

Kelven 明确描述交付体验：在 Telegram 开始对话，Agent 就在该 Telegram 对话给出网站地址，让用户顺利进入预览；不要求进入或适配 Control UI。用户随后更正口误，所指是 **Control UI**，不是“群组 UI”。

- Telegram 承担对话和链接交付，独立网页承担模型预览及既有标注／提交功能。无需先切换到 Control UI，也不新增 Telegram 专用嵌入界面作为交付条件。
- 结合既有原会话原则，本次由 Telegram Coding 发起的审阅，其标注、修改说明及 Agent 回复应回到同一话题；不能沿用旧 Control UI 目标而声称完成 Telegram 接线。
- 可用链接以用户实际使用的设备和网络能访问、能加载正确模型为准；只有 Agent 主机能打开或只发出地址，不算用户预览交付完成。`127.0.0.1` 不能当作其他设备可用的入口。
- 09-10 13:46 用户进一步明确同一内网的 Windows → MacBook Pro 访问需求。外出跨网络／公网支持仍未确认，未选择域名、隧道或云部署方案。
- 当前只讨论和记录定位，未启动服务、开放网络、修改回传绑定或验证新链接。既有版本锁、草稿和不可变提交保护保持。

## 内网访问与临时授权（2026-09-10 13:46）

### 用户明确的要求

- 工作台在 MacBook Pro 运行，Windows 电脑通过其内网地址访问；Agent 给出的地址应对应实际可达的内网接口，不能仍交付 Windows 自己的回环地址。
- 访问须有临时授权，防止其他内网设备扫描到端口后直接查看／篡改标注。用户提议 URL 携带临时 token，Agent 每次重新给出入口时更换一次，不使用长期 API／Gateway 密钥。
- 用户明确愿意在私聊或群聊接收该临时 token，并不希望按长期高敏感密钥处理。13:54 再次强调愿意接受其有限破坏面，用户体验优先，要求点收到的链接即可进入，不增加配对／确认步骤。用户要求已经明确，不再写成用户尚未决定；下节单列当前环境不能直接执行的部分，不等同已实施或可绕过宿主限制。

### 当前适配约束与待定方案

- 当前运行环境禁止 Agent 将访问凭证（包括临时 token／配对码）放进聊天或 URL。只要参数本身足以取得模型／标注权限，它就是访问凭证，不能改称“编号”后继续明文投递；本轮不生成或发送任何真实凭证。
- **13:54 已撤下配对建议**：此前“普通链接＋Windows 经 Mac 本机确认配对”的建议不符合用户明确的一键进入体验，不再作为待采纳方案或实施默认。产品的一键进入要求与当前凭证交付限制分开记录；不能以改叫分享编号、附件或自动投递等方式间接传递有效凭证。当前尚无已核实且同时满足这些边界的交付实现，不宣称限制已解决。
- 每次发放使用新授权是用户要求；有效期、旧入口何时作废、已打开编辑会话如何续接仍待定。建议更换入口停止旧入口的新接入，同时保护已保存草稿及活跃审阅，不自动换模或清锁；建议不冒充已确认要求。

### 实施与验收边界

- 必须保护模型／贴图／下载、草稿、提交与状态等受保护资源和接口，不只检查 HTML 首页。无有效授权的内网请求不能取得这些数据或写入标注；可公开的入口壳本身不构成授权。
- 当前源码仅监听 `127.0.0.1`；写接口的 `x-review-client` 固定标头不是身份认证，不能靠改监听地址就宣称内网安全交付完成。Agent 发布模型的本地 Unix socket 不因开放浏览器访问而暴露。
- 需分别验收 Windows 实际加载和操作、未授权直接访问资源／接口被拒、新授权更换／到期语义、草稿与版本锁保全，以及提交准确回原 Telegram 话题。
- 本轮只更新设计；不改监听、配置、服务或真实模型／草稿／提交。用户体验要求已明确，当前适配交付限制仍未解决；不以被撤下的配对方案开始编码，也不要求用户再次确认相同偏好。

## 保留与取消

| 项目 | 新基准 |
| --- | --- |
| 独立工作台核心、统一参数模型／人偶审阅 | 保留 |
| Agent 交入模型、读取结构化标注、发送理解回显与下一版 | 保留，通过平台支持的工具／接口适配 |
| 原会话绑定、真实回执、版本锁、草稿与旧提交保护 | 保留，不因更换显示入口而降级 |
| 共用核心、各 harness 包装，装一个扩充即可用 | 保留；套件负责必要启动与接线，打开标准浏览器，不让用户手动拼装 |
| OpenClaw／Codex／ChatGPT 等内置浏览器、WebView 或侧栏嵌入 | **取消，不列作本轮或后续产品兼容目标** |
| Control UI Portal 作为交互入口 | 取消待办，不以另一种宿主入口替代已取消的侧栏要求 |
| 为工作台维护宿主截图流、鼠标转发、下载中转补丁 | 不再作为产品依赖、发布前提或交付内容 |

取消的是**显示入口适配**，不是取消 OpenClaw／Codex 等的 Agent 接驳。未来 Skill、MCP 或插件仅承担工具接线与交付职责，不意味着要提供内置浏览器。

## 发布与维护边界

- 工作台升级应独立于宿主核心补丁与 Gateway 重启。工作台自身服务如需更新或重启，按其部署流程处理；平台要求的普通扩展加载条件另行核实，不能承诺所有平台永远无需重启。
- 0.3 核心候选与既有验收材料保留。旧「等 Gateway 重启 → 发布侧栏下载配套 UI → 发布工作台」流程**不再执行**，也不再以该重启作为标准浏览器版的发布前提。
- 已落盘的宿主补丁、未发布的 UI 候选及配置记录保持可追溯；**本次只更新文档，不回滚、删除、重发补丁或改配置，不重启任何服务**。如之后清理遗留部署，须先核对实际状态并单独安排，不自动执行旧脚本。
- 决策后的进度：2026-09-10 已按新范围独立启用工作台 0.3，Chrome 加载与原档下载核验通过，未重启 Gateway；见 [发布记录](STANDARD-BROWSER-V03-20260910.md)。不因此宣称 Agent 接线已完全解耦或其他浏览器全部兼容，安装包仍不开发。

## 新验收基准

1. 从原会话获得工作台入口，在用户自己的标准浏览器打开当前模型，完整操作预览、标注、清单、擦除／填色、显示模式与理解回显；布局兼容普通及并排窄窗口。
2. 提交携带正确模型身份、三维位置与批次，进入发起会话；保存、送达、读取各有真实证据。单有网页链接不代表回传闭环通过。
3. 点击下载后，文件由用户正在使用的浏览器接收；核对内容对应点击时实际显示的版本，保留原材质，不包含审阅覆盖层，不改变草稿或锁。
4. 验证上述完整路径不依赖已打补丁的 Control UI 内置浏览器。旧侧栏测试只作历史证据，不能替代标准浏览器与 Agent 接线验收。

关联：[定位](POSITIONING.md) · [需求 R14／R16／R28](REQUIREMENTS.md) · [路线图](ROADMAP.md) · [0.3 状态](ITERATION-V03.md)。
