# 需求与验收方向

项目入口：[PROJECT.md](PROJECT.md)。2026-09-09 立项初稿。

**2026-09-09 定位补充**：用户确认本产品面向不同 Agent harness，OpenClaw 仅为首个适配实现。以下提及 OpenClaw 的交付与回传要求，在通用定位下属于 harness 适配职责，不要求其他平台使用 OpenClaw；详见 [POSITIONING.md](POSITIONING.md)。本次仅更新定义，不宣称通用适配层、MCP 或 Skill 已实现。

**2026-09-10 入口范围更正**：R14 已替换为标准浏览器直接交互，取消所有 harness 内置浏览器／侧栏适配；R16、R17、R27、R28 及验收范围同步调整。见 [入口决策](BROWSER-ACCESS-DECISION.md)。以下带 09-09 日期的讨论及「未实现」描述保留当时状态，候选实现进度以 ITERATION-V03.md 为准，不能据历史侧栏要求恢复宿主部署。

## 用户已确认

| 编号 | 需求 |
|---|---|
| R01 | 本地网页加载模型，支持旋转、缩放预览。 |
| R02 | 同一界面支持参数化模型和 Tripo 人偶，不按来源拆成两套工具。 |
| R03 | OpenClaw 将生成的模型交给网页展示，仍负责建模工具调用。 |
| R04 | 点标签统一使用大写字母 A、B、C、D…，不再提供数字标签选项；落点手势按 R15。同一轮审阅中删除不重排、不复用已分配字母：A/B/C/D 删除 B 后保留 A/C/D，新增点为 E。此为新设计，尚未实现，不改写既有数字标注。 |
| R05 | 鼠标画笔、不同颜色标记三维区域；默认只选可见表面，不穿透选择。显示及回传范围必须跟随实际笔迹，不可因触及一个三角面而将整面填色；区域按颜色称呼，不冒充模型上的数字／字母点标签。 |
| R06 | 一键反馈编号、位置和标色区域给 OpenClaw。 |
| R07 | 用户随后在同一会话用文字说明「A 点怎样改、B 点怎样改」；不依赖开启语音。历史提交仍按其原标签引用，不静默重命名。 |
| R08 | 审阅网页只负责模型显示、标注与提交，移除左侧复制会话及聊天输入框。发起审阅的原会话是唯一对话入口；标注回传、修改说明及 Agent 回复均绑定原会话，不额外复制或分流到另一处。Telegram、Control UI 及未来 ChatGPT App 是不同来源入口，不代表目前已全部适配。 |
| R09 | 单一大视窗只显示一个当前版本，由 Agent 控制显示与更换；不提供用户自行切换历史版本的主流程，避免模型与标记混淆。 |
| R10 | 网页不同时常驻多份历史模型，保留资源占用限制；单模型规模及切换时的资源释放策略待定。 |
| R11 | 提交后 Agent 确认已收到这批标记；若缺少文字说明或标记含义不清，在原会话主动询问，获得足够说明后再修改，不仅凭颜色／编号猜测修改意图。 |
| R12 | 用户正在新增或修改标注时，Agent 不得切换／替换当前模型，即使新旧差别很小；保护未提交标注，不让用户做到一半的工作丢失。 |
| R13 | 在用户开始审阅前，由 Agent 负责确保交付并显示正确的最新版本；让用户误在旧版上工作属于 Agent 的版本同步问题，不能要求用户自行识别或承担后果。 |
| R14 | 正式交互入口统一为用户自己的标准浏览器窗口／标签页，直接渲染模型、处理手势及接收下载。取消 OpenClaw Control UI、Codex、ChatGPT 等 harness 内置浏览器／侧栏／WebView 适配，不以 Portal 替代此要求；原会话与工作台可以并排使用，但不要求同页嵌入。具体浏览器与版本支持范围须验证。此条于 2026-09-10 取代旧侧栏要求。 |
| R15 | 旋转观察与点标签无需切换工具。用户指定触控板／鼠标操作：按住左键拖动旋转模型，对模型表面某点双击落一个标签；普通单击不落标签。取代助手先前提出的单击落标签方案。 |
| R16 | 定位为可适配不同 Agent harness 的通用 3D 审阅与标注工具；核心模型／标注／版本保护与平台接线分责，保留原会话及其 Agent。OpenClaw 为首个实现，不是强制平台。保留其他 harness、MCP、Skill 的工具接线方向，不包含宿主内置浏览器适配；标准浏览器交互与原会话回传分别验收，不将未来方向写作已实现能力。 |
| R17 | 正式产品只提供面向无开发经验建模用户的一体化安装流程，不设「进阶用户自行组装」选项。不要求用户运行开发命令、编辑配置或分别安装程序／Skill／MCP；依赖、启动、平台工具接线与兼容检查由整合流程处理。安装后从原会话取得标准浏览器入口，不把宿主核心补丁或内置浏览器改造作为安装要求。验收须验证浏览器可操作及原会话回传，不仅是文件落盘。具体安装包、操作系统及版本范围待定，当前未实现。 |
| R18 | Agent 理解回显展示其认为要修改的范围，不是改好后的效果图。独立高亮层可隐藏，保留用户原标注与模型，原会话附简明解释；定位不准先澄清，不伪造选区。高亮出现不改变当前视角，用户主动点击「睇修改範圍」才定位。设计已确认，尚未实现。 |
| R19 | 提供全局显示／隐藏切换按钮：一次点击收起全部用户点标签、涂抹笔迹及 Agent 理解高亮，再次点击恢复显示，方便预览干净模型，不必逐项操作。隐藏属于临时显示状态，不删除标注、取消修改要求或过滤提交内容；模型及原有标注数据保留。设计已确认，尚未实现。 |
| R20 | 用户误删标记后可以撤销删除；恢复的是同一个原标记，保留原字母、位置及标注数据，不重新分配字母、不当成新增点。例如删除 B 后撤销，恢复的仍是 B，其他标签不重排。基础撤销重做已有实现，本条为用户确认的字母标识保留要求，须随新设计集中核验，当前未修改程序。 |
| R21 | 提供可收合的标注清单，便于标记较多时查找，并可收起清单腾出模型显示空间。点选清单中的 C 只高亮对应 C 标记，不改变视角；用户另行点击「定位」才移动视角查看该标记。此为用户已采纳的设计，当前未修改程序，待后续集中开发验收。 |
| R22 | 提交回馈明确区分「已保存」「已送到原会话」「Agent 已读取」，每一状态都须由对应事实／真实回执支持；不因用户点击提交、经过一段时间或前一步成功就宣称后一步完成。Agent 已读取不等于已理解、已开始改模或修改完成。用户已采纳此设计，当前只记录要求，待集中开发核验。 |
| R23 | 提交后仍可继续新增或修改标记，但已提交的那批内容保持不变。后续改动留在当前草稿并显示「尚未提交」，必须由用户再次点击提交才交给 Agent，不自动送出。保存草稿与发送到原会话分开；旧批次的送达／已读回执不表示新改动已经提交。用户已确认，当前只更新设计，待集中核验实现。 |
| R24 | 画笔可切换为标注橡皮擦，局部擦除已经画上的标注笔迹，不必删除整个色区；只改标注涂色，不破坏模型几何或原始材质。用户已确认需要此功能，当前只记录设计，尚未开发。 |
| R25 | 增加标注油漆桶，一次填色一片表面，而不是仅填单个三角面。相邻三角面衔接方向差很小时可跨面扩张，同时限制候选面相对最初点中面的方向偏差，避免沿平滑曲面因局部小角度累计而越填越远。两项角度条件须同时满足；不要求严格数学共面。填色属于标注层，不改模型原材质。此范围规则已获用户确认；具体容差及遮挡等细节待定，尚未开发。 |
| R26 | 支持标签重新定位：在清单选择某标签的重新定位操作，再点击模型上的正确位置，移动的是同一个标签，保留其字母与标识，不先删除再创建。例如 B 移动后仍是 B。操作放在清单内，不另加常驻工具按钮；已提交批次保持原样，新位置按 R23 留草稿、再次手动提交才送出。用户已采纳，当前未开发。 |
| R27 | 页面布局以模型预览为主要工作空间：明显缩小顶部标题区，压缩上下留白及其他非必要纵向占位，避免模型窗口被推到页面下方或过小；在标准浏览器普通窗口或并排窄窗口内尽量扩大模型预览区域。保留响应式布局，不将窄窗口支持等同宿主侧栏适配；具体手感须实测。 |
| R28 | 提供直接下载当前预览版本的按钮。用户审阅满意后可自行取得眼前这版模型，不必再要求 Agent 整理文件；下载目标绑定当前实际显示的模型版本，而不是后台最新、待发布或其他历史版本。下载不代表提交标注、结束审阅或切换模型，不要求该版本先被标为最终版。用户已明确提出，尚未实现。 |
| R29 | 审阅对象不只素体：带着色／贴图的模型保留原有颜色与材质外观，以原色预览为主要模式；标注通过图案及对比描边等与原色区分，不只依赖颜色。支持自选的素色辅助检视，不强制替代原色审阅；标注及检视模式均不改写原材质或下载文件。用户已采纳此呈现方向，具体样式待细化及试用，当前未改程序。 |

**R30 · 原对话中的可用链接交付（2026-09-10 用户明确）**：用户在 Telegram 发起对话，Agent 在同一对话提供能顺利进入模型预览的网站地址；不要求转到 Control UI 或适配其界面。入口须在用户实际设备／网络加载正确模型，不能以仅服务主机可用的地址冒充交付；标注及回复仍按 R08 回原话题。内网访问已按 R31 明确，公网／外出跨网络未确认；尚未实施访问方案或迁移回传绑定。详见 [入口决策](BROWSER-ACCESS-DECISION.md)。

**后续实现状态**：R30／R31所述领取缺口已有[内网定向适配](LAN-ADMISSION.md)，由 Agent 核对客户端地址后签发，网页自动取得 HttpOnly 会话；此实现不将用户的 token 链接偏好改写为已撤回，也不宣称地址核对是强设备认证。Windows 页面连通已获用户确认，实际模型与原话题回传待端上验收。

**R31 · 内网跨设备访问与临时授权（2026-09-10 13:46 用户明确，13:54／15:32 补充）**：支持 Windows 通过内网地址访问 MacBook Pro 上的工作台；模型与标注操作受临时访问授权保护，每次重新发放入口更换授权，不能仅靠内网位置或隐藏端口。用户明确要求收到链接后直接进入，愿意接受短期 token 在私聊／群聊传递的有限风险，用户体验优先，不增加配对／确认；此前配对替代建议已撤下。15:32明确认可「15分钟一次性入场许可 → 取得许可后建立60分钟浏览器会话；重新发行不打断已进入的编辑视窗」。新许可作废旧未用许可，活跃会话保留原身份及到期时间；保留版本锁及草稿，不开放本地 Agent 管理接口。0.4授权核心已实现及隔离验证。当前环境仍禁止 Agent 在聊天或 URL 中传递有效访问凭证，实际一键入口投递尚未交付；用户认可机制不等于此门槛已经解决。

R25 交互补充已获用户确认：在油漆桶工具下，鼠标移到模型表面先预览将填色的范围，点击一次才正式应用标注颜色，不额外弹出确认窗口。预览本身不修改草稿、不提交给 Agent；正式填色仍遵守 R23 的手动提交边界。当前只记录设计，未实现。

R25 范围滑杆纳入待试用设计：用户同意先加入滑杆，便于将预览范围收窄／放宽并即时查看变化，不要求输入角度数字；同时明确担心工具过多、界面复杂，须待实际使用后评价，不能把纳入试作当作最终可用性验收。滑杆与两项角度约束的映射、默认值及布局尚待细化；调节仍受相邻面与固定起始面两项规则约束，不另开无限扩张模式。当前并未授权立即开工，仍按本轮先收集设计再集中开发的安排执行。

R16／R17 后续设计基准已获用户确认：共用核心，OpenClaw／Codex 等平台分别包装；用户体验是「安装一个扩充，再在原会话请求打开模型审阅」，通过标准浏览器使用，不另行管理工作台进程。实际套件不限定为纯 Skill；平台工具接线、安装及标准浏览器交互分别验收，不要求修改宿主核心代码。安装包与新接口实现留待安装交付阶段，详见定位说明。

2026-09-09 先共同讨论蓝图；同日后续用户已明确授权在休息期间多轮迭代完成可测试初版。此前「不开始写代码」是已结束的讨论阶段，不再阻止本轮实现。

## 设计更正记录

2026-09-10 用户明确取消内置浏览器适配：工作台以标准浏览器为正式交互入口，不再为 OpenClaw／Codex 等宿主维护模型显示、手势或下载转发。原会话仍是唯一聊天入口，Agent 接驳、版本锁、真实回执和一体化安装方向不变。此前 R14、侧栏下载验收、Portal 待办及依赖 Gateway 重启的旧发布顺序均已被取代；只改蓝图，不回滚补丁、改配置、重启或部署。

2026-09-09 彩色模型与标注辨识：用户指出人偶可能自带着色，必须能看原模型颜色，同时询问如何处理它与标注颜色的冲突，记为 R29。本项目此前已有保留原材质的设计及带材质模型路径，本次不将其描述成只支持素体；新增重点是彩色背景上的标注辨识与实际体验验收。

随后用户已采纳「保留原色／贴图，标注加图案与描边，一键隐藏查看模型，素色检视只作辅助」的呈现方向。以下细化以此为基准；图案密度、线宽与具体控件布局等尚未定案，不表示已实现：

- 保留原色／贴图作为正常审阅视图。字母标签采用清晰的实色底与对比描边，不只依靠标签颜色区别。
- 笔迹／填色区同时使用标注色、稀疏斜线或点纹及清晰边界；避免只叠半透明颜色导致与原色混合后难以识别。同色冲突时调整对比样式，不擅自把用户约定的红色区域改名或换成另一种语义颜色。
- Agent 理解回显继续独立，通过轮廓／线型等与用户标注区分，不只再增加一种可能冲突的颜色；其具体样式仍待定。
- 自选的素色辅助视图已获用户认可，只临时弱化原材质显示，方便看形状与选区；不是删除贴图、修改模型或代替原色审阅。切回原色时恢复原材质显示，R28 下载仍提供该版本原模型而非素色检视结果。拟放在查看设置中避免增加常驻工具，具体入口未定。已有 R19 全局隐藏仍可用来无标注地观察模型。

2026-09-09 用户新增布局与下载反馈：

- R27：现有最上方标题占位过大，令真正用于操作的模型窗口靠下且偏小；应压缩标题及上下非必要空间，优先保留模型面积。拟采用紧凑标题／必要版本和状态信息，减少大块说明与留白，工具结合上下文和可收合清单安排；具体尺寸与样式未定，不以扩大模型为由移除必要状态或降低可操作性。
- R28：下载当前预览版本应由用户直接在工作台完成，无需再往返要求 Agent 打包。下载引用须固定到用户点击时实际显示的模型版本及其文件，后台另有新版不改变目标；模型版本信息须可辨认。
- 下载默认设计为该版本的模型文件，保留原始材质，不把审阅标签、笔迹或 Agent 理解高亮烘焙入模型，也不以浏览器用于显示／选区的临时细分网格冒充交付文件。多格式转换、源工程及打包导出并非本次已确认范围，另行设计。
- 下载必须在用户自己打开工作台的标准浏览器中验收能取得并打开原文件，不能仅有服务端记录或下载事件就算交付。此前远端侧栏下载转送要求已于 2026-09-10 取消；不再为各 harness 分别实现下载中转。
- 两项继续纳入本轮集中设计，不因这次反馈而立即改代码、下载实际文件、切换模型或部署服务。

2026-09-09 标签重新定位：用户明确同意 R26，从清单发起更改标签落点，保留原字母，不影响旧提交。须在界面区分 R21 的「定位」与本操作：前者只调整相机以查看标记，后者实际修改当前草稿的标记位置，不能共用含糊入口使用户误操作。具体按钮措辞、取消与误操作恢复细节待细化；原观察／双击落点手势不因本条自动重定义。当前只记录设计，不移动现有标记或修改程序。

2026-09-09 橡皮擦及油漆桶：用户确认 R24 局部擦除，并主动提出 R25 整片表面填色，特别强调「不是一个三角面」，随后说明相邻三角面衔接角度很小应认作同一片平面。这里的角度指表面方向差（法线夹角），不将操作退化成原来误填整三角面的画笔行为；画笔仍按 R05 实际笔迹范围着色，油漆桶是独立的主动填充操作。

- 已确认：画笔／橡皮擦切换、局部擦除；油漆桶跨相邻且方向接近的多个三角面选取表面区域，并同时约束相对起始面的方向偏差。
- 待讨论：两项角度容差数值及与范围滑杆的映射、平缓曲面的可接受范围、油漆桶对遮挡部分的处理、擦除对重叠颜色的作用范围；不能因加入油漆桶就擅自放宽画笔可见面规则。是否加入范围调整已获用户同意先试，不再列作未决定。
- 后续已获用户确认：不能只按相邻法线差逐步扩张，还须限制相对最初点中面的方向差，避免沿光滑圆柱／球面扩展过远。扩张时以固定的起始面为比较基准，不能每一步改用当前邻面而丢失累计方向限制。范围滑杆已纳入待试用设计，具体容差及映射仍待定，不改成零容差的严格共面选择。
- 预览交互随后获用户确认：油漆桶悬停显示待填范围，单击应用，无额外确认弹窗；这是工具自己的临时预览，不是 Agent 理解回显。仅在油漆桶模式使用该单击规则，不改变 R15 的观察／双击点标签手势。
- 滑杆与复杂度：用户同意先加入范围滑杆，最终以实际试用判断是否顺手。设计拟将调节项放在油漆桶工具上下文内、用到才显示，避免堆满常驻工具；此为布局建议，具体实现与手感未定。操作滑杆时如何保持预览目标等细节须后续细化；必要时依用户试用反馈简化，不因已经列入需求而拒绝调整。
- 当前只累计要求，不修改已有标注、历史提交、模型、程序或服务。

2026-09-09 提交后继续编辑：用户确认 R23，原提交批次作为固定记录保留，新改动必须明确标示尚未提交并由用户主动再提交；不因为草稿保存成功就自动通知 Agent 或改写先前批次。R22 的已保存／送达／已读事实应关联各自批次，不能掩盖当前尚未提交的改动；提交仍不结束审阅或解除模型锁。后续提交采用完整快照还是增量、怎样向 Agent 表达与前批的关系尚待设计，不在本次定案中预设。

2026-09-09 提交回馈：用户明确采纳 R22 三种状态及真实回执要求。落实时须区分持久保存成功、标记提交确实进入绑定的原会话、Agent 实际读取该批标注；仅传输请求受理／排队不能冒充送达或已读。回执必须对应正确批次及模型版本，其他批次的旧回执不能推进当前批次状态。没有相应证据时不能显示该状态；异常／超时呈现与重试交互待后续设计。现有基础提交机制不等于这套状态呈现已完成验收。本次不改模型、标注、程序或服务。

2026-09-09 标注清单：用户明确采纳可收合清单、点选高亮与单独定位的设计，记为 R21；不把点选清单等同于自动转视角，也不新增聊天入口。具体清单布局、笔迹区域条目的命名／归组及全局隐藏时点选条目的呈现规则仍待细化，未自动定案。

2026-09-09 撤销讨论：用户确认撤销删除应当具备，并同意恢复原标签字母。R04 的「不复用」指不把已分配字母给另一个新点，不阻止恢复同一个原标记。撤销仅作用于当前审阅草稿，不改写已经提交的历史快照。其他撤销范围、跨提交边界及入口细节尚待细化，不在本次确认中扩展为新功能授权。

2026-09-09 隐藏功能讨论：用户强调全局一键隐藏，而不只是单项隐藏；希望模型画满标签和笔迹时能方便查看干净模型，已记 R19。随后明确确认同一按钮切换隐藏／显示，Agent 理解高亮也一并收起。该动作仅切换覆盖层显示，不改变视角或模型；R18 中理解回显可独立隐藏的规则不变。隐藏期间新增标记及全局切换与单层可见状态的恢复细节尚待细化。当前仍只收集设计，不修改程序或标注。

2026-09-09 标注管理讨论：用户确认 R04 统一为大写字母，并明确删除 B 后下一点是 E，不补回 B，避免原会话引用错位；取代先前数字／字母二选一的目标设计。涂抹区域仍按颜色识别，不套用点标签字母。当前只记录需求，不改程序、草稿或历史提交；已有 1/2/3 号测试标记保持原样。超过 Z 的延续方式、跨轮是否重新起算及后续旧草稿兼容细节尚待讨论，不作已确认方案。

2026-09-09 用户实际试用后更正 R08：无需网页内的重复聊天框，原会话继续承担全部对话；网页可保留保存／提交状态，但不复制聊天记录。此决定取代早期左右布局。用户同时报告笔刷整面扩色问题并明确 R05 的笔迹边界要求；随后用户已收齐反馈并授权改进，0.2 已完成隔离验证，并于 15:36 激活。用户新手感及 Control UI 真实侧边操作仍待验收；见 ACCEPTANCE-V02-20260909.md。

2026-09-09 后续语音中，用户明确撤回自行切换多个历史版本的方案，改为 Agent 控制唯一当前显示版本；此前记录仅属历史决定，以 R09 当前内容为准。

## 验收方向（初版完成情况见 ACCEPTANCE-20260909.md）

- 同一页面分别显示参数化样例及带材质的人偶样例。
- 旋转模型后，编号仍指向相同表面位置，不是固定屏幕涂鸦。
- 字母标签新设计验收：同轮建立 A/B/C/D 后删除 B，A/C/D 标签与位置不变，下一新点为 E；恢复同轮草稿后仍不回收 B 或重排。此项待后续集中开发验证，不表示当前版本已支持；历史提交引用不得被改写。
- 撤销删除新设计验收：删除 B 后执行撤销，恢复同一个 B 的标识、原位置与数据，A/C/D 不变；恢复 B 不当作新点或分配 E，下一真正新增点仍按 R04 递增。已提交历史保持不变。现有基础撤销不等于这套新字母规则已验收。
- 标注清单新设计验收：展开清单后点选 C，高亮正确的 C 标记且相机不动；明确点击「定位」后才调整视角。收起清单释放显示空间，不删除标注或改变模型／提交归属。此项待集中开发验证。
- 标签重新定位验收：从清单对 B 发起位置更改并点击新表面位置，B 的显示与三维落点同步更新，原字母／标识不变，其他标签不重排，亦不消耗新的字母。已提交记录仍指向旧位置，新位置显示为尚未提交并仅在用户再次提交后发送；R21 的查看定位不得移动标签。此项待集中开发验证。
- 紧凑布局验收：在标准浏览器普通窗口和并排窄窗口下，顶部标题和上下非必要占位明显减少，模型可用高度／面积增加，不再被大标题推到下方；关键工具、版本及状态仍可辨认和使用。按用户实际预览操作评估，不仅验证 CSS 数值变化，不要求宿主侧栏测试。
- 当前版本下载验收：界面显示版本 X、后台存在待发布版本 Y 时，点击下载得到 X 对应的模型资产；原文件直下时校验其内容哈希与 X 记录一致。用户自己的标准浏览器能够取得并打开文件，模型几何／原材质正确且没有审阅覆盖层；下载不改变模型锁、当前版本、草稿或提交记录。不依赖宿主下载补丁。
- 彩色模型审阅验收：使用带贴图、多色及与标注同色的样例，在原色视图中能通过标签对比描边、区域图案与边界等辨认标注，不能仅依赖色差；全局隐藏标注后原色／贴图不受破坏。用户可切换素色辅助并恢复原色，过程中几何、原材质、标注归属及下载资产保持不变。具体样式由后续设计及用户实测细化，不以素体样例测试代替此项。
- 提交回馈新设计验收：只保存成功时不能显示送达或已读；送到原会话但 Agent 尚未读取时不能显示已读；获得该批标注的实际读取回执后才显示「Agent 已读取」。延迟／缺失及其他批次回执不能造成虚假推进，也不据此改模或解锁。此项待集中开发验证。
- 提交后编辑新设计验收：提交 A/B 后再新增 C 或修改 A，原提交记录不变；界面显示存在「尚未提交」改动，草稿保存不触发送出，旧批次已读回执不能清除该提示。用户再次提交后，才按新批次的真实回执更新送达／读取状态。全过程不因提交或继续编辑而替换模型。此项待集中开发验证。
- 橡皮擦设计验收：从一块标注色区中擦走局部笔迹，其余标注保留，模型几何／原材质与历史提交不变；当前草稿改动按 R23 处理。待集中开发验证。
- 油漆桶设计验收：由多个三角面组成的一片平面及带细小方向误差的同类表面，可在约定容差内跨三角面填色，而不局限于点中的单个三角面；显示与提交选区一致。在细分圆柱／球面上，即使每对相邻面均满足局部角度条件，候选面相对固定起始面的方向差一旦超过约定上限就不再纳入，不能沿曲面无限累计扩张。具体容差与遮挡边界验收待细化；不得把旧整面画笔问题当成油漆桶已实现。
- 油漆桶预览验收：未点击时只显示当前待填范围，不改标注数据或发送消息；点击一次后实际填色范围与当前有效预览一致，不额外弹窗。切换目标后不得将旧预览范围错误应用到新点击位置。填色后如有新改动，应标示尚未提交而非自动送给 Agent。待集中开发验证。
- 范围滑杆待试用验收：对同一预览目标调节可收窄／放宽待填范围，反馈及时，不要求手输角度；调整本身不落色或提交，正式落色对应最新有效预览。功能测试之外，须由用户实际评价工具数量、操作负担及模型显示空间，再决定最终保留／简化方式，不能只因功能存在就认定好用。
- 全局隐藏新设计验收：多个点标签、多色笔迹及 Agent 理解高亮均显示时，点击一次收起全部覆盖层，模型本身仍可预览；再次点击同一按钮恢复显示。切换不改变视角、模型或提交所包含的标注集合，不丢失标签、位置、笔迹及理解回显数据。此项待后续集中开发验证。
- 同一观察状态下，左键拖动只旋转，不误落标签；表面双击只新增一个标签，不重复落点、不同时触发缩放／聚焦；空白处双击不落标签。不要求返回工具栏切换观察与点标签。画笔手势另行设计，不把此规则直接套为绘画动作。
- 在用户自己的标准浏览器中实际验证载入、旋转、点标签、笔迹、提交、理解回显与下载；原 Agent 会话保持可用，回传不串会话。新 harness 验证其工具接线与标准浏览器闭环，不再验收内置侧栏；仅打开链接不等于完整功能通过。
- 反馈含结构化三维位置／选区及模型版本，截图只作辅助。
- 反馈绑定正确会话，不串模型、串版本；Agent 可高亮回显理解范围。
- 参数化修改回到原始工程，再生成预览；人偶修改路径须实际验证。
- 重新生成或重拓扑后，不把旧面索引默默套到新版上。
- 标注可保存、恢复，能查看修改后的版本。
- 用户正在标注时，即使后台产生新模型，当前模型和标注仍保持不变；待安全交接后再由 Agent 更新。
- Agent 交付模型与查看器实际显示的版本应可核对；若发现错版，保留原版标注及归属，由 Agent 跟进，不静默套到新版或清空草稿。

## 待决，不作已拍板方案

### 新一轮设计讨论：Agent 理解回显（2026-09-09）

用户认可「标注与模型对照、Agent 理解回显、真实提交状态」三个优化方向，选择先讨论理解回显，并确认R18的呈现与视角规则。本轮不开始编码。

用户进一步明确：本轮还要讨论多个改进点，先累计并整理，待收齐后再一次性集中开发；每一项设计确认只更新要求，不立即触发程序修改。当前讨论状态见ROADMAP顶部。

- 例如用户在孔边落点并要求加大，Agent在可靠定位后高亮理解中的孔范围，同时在原会话解释修改意图；高亮不代表几何已修改或所有参数已澄清。
- 原标注继续作为用户输入保留，Agent回显独立显示／隐藏，不覆盖点标签或笔迹，也不解除审阅锁或切换模型。
- 更正流程已获用户确认：直接在原会话以文字或语音指出「范围太大」「不是这个孔」等偏差，Agent据此修订自己的高亮层；无需用户删除原标记并重新标注。说明仍不足时先澄清，不凭更正请求直接改模型。
- 出现回显时不抢视角；仅用户主动点击定位操作才移动视角。原会话更正方式已定，具体高亮样式、批量范围的呈现与对应、回显状态规则仍待讨论，不作已定案方案。

### 其余待决项

- 历史文件后台清理／回退策略尚待后续设计；初版保留文件，界面不提供自行切历史版。
- 初版采用「已提交后，由用户点击结束本轮审阅」作为明确交接点；提交本身不解锁。这是实施选择，待用户试用反馈。
- 初版使用 Three.js 轻量层；GLB／STL 上限 80 MB、60 万源三角面，并限制贴图解码预算。其他格式待扩展。
- 初版回传采用本地 JSON + `chat.send`／`chat.history`，绑定原会话，`deliver:false`；这是旧实现描述。按 R08 改造时移除网页聊天历史／输入，标注仍绑定发起会话；不同入口的回复路由须分别验证，不把一种路由直接套到所有平台。后续是否扩展 MCP 未定。
- 受支持的标准浏览器／版本矩阵，以及各 harness 提供链接或打开系统浏览器的方式、工具接口和维护成本；不重新引入内置浏览器适配。
- CAD 显示面到源几何／参数的映射；版本更新后的批注重新定位。
- 默认可见面选择已确定；是否另外提供非默认穿透模式尚未确定。
- 审阅颜色独立于原材质／打印颜色，避免误改母版。

## 当前不包含

不新造完整 CAD／雕刻软件，不更换 OpenClaw 主 Agent，不收窄为城市分割或人偶专用。不承诺 Tripo 已支持任意选区精确修改，也不承诺所有模型可自动修成可打印成品。

不适配 harness 内置浏览器／侧栏／WebView，不创建 Control UI Portal 作为产品入口，不将宿主截图流、手势或下载转发补丁作为工作台交付要求。已有历史补丁的清理不在本次文档修改范围。
