# Local environment and credentials
.env
.env.*

# Machine-local files, dependencies, and build outputs
.DS_Store
node_modules/
.venv/
__pycache__/
*.py[cod]
/dist/
/build/
/tmp/
*.log

# Historical delivery payloads include local paths and session identifiers.
# Keep the files locally; track their human-readable summary in REFERENCES.md.
/evidence/20260909-media/*.json

# Runtime state and browser verification outputs
/runtime/
**/browser-access.json
**/browser-access.json.*.tmp
/test-results/
/playwright-report/

# Machine-specific installed-code backups and staged runtime bundles.
/evidence/sidebar-release-*/
