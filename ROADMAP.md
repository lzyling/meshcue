# 阶段与待办

## 当前工作方式：先集中讨论，再成批开发（2026-09-09）

Kelven 明确要求继续讨论多个产品改进点，不因某一项设计确认就立即修改程序；待本轮改进点收齐，整理范围、优先级与验收要求后，再集中进行开发。当前仅维护设计与待办，不启动逐项实作、安装包开发或服务变更。

- **已确认细节**：Agent理解回显采用独立可隐藏高亮，不覆盖原标注／模型；保持视角，用户点选才定位；用户在原会话更正偏差，Agent只修订自己的高亮，无需重新标注。见需求R18。
- **已确认的点标签规则**：仅用大写字母；同轮 A/B/C/D 删除 B 后留 A/C/D，新增为 E，不重排或复用。见需求R04。仅设计确认，程序和旧数字标记未改；超过 Z、跨轮起算等细节待讨论。
- **方向认可、细节待讨论**：标注列表与模型对照、定位和单项管理；有真实回执的提交／处理状态提示。
- **已确认的全局隐藏**：同一按钮切换隐藏／显示全部用户点标签、笔迹及 Agent 理解高亮，便于预览干净模型；不是删除或取消提交。见需求R19。隐藏期间的标注操作及单层可见状态恢复细节待讨论，未开始实现。
- **已确认的撤销删除**：误删后恢复同一个原标记及其字母／位置，删 B 再撤销仍是 B，不当成新点或重排其他标签。见需求R20。基础撤销已有实现，新字母规则须集中核验，本次未改程序。
- **继续收集**：其余用户意见、高亮样式、多范围对应、回显状态及异常流程。讨论中的建议与已确认要求分开，未确认细节不自动定案。

下方包含早期实施阶段记录，不能据其中旧授权自动提前启动本轮新增优化。

项目入口：[PROJECT.md](PROJECT.md)。2026-09-09 用户已授权多轮迭代完成可测试初版；当前完成范围见 [验收记录](ACCEPTANCE-20260909.md)。

## P0 · 立项整理 — 已完成

- [x] 正式项目目录、主文件和记忆索引。
- [x] 项目级本地 Git 版本管理及初始文档快照；未配置远程仓库。
- [x] 统一审阅界面的需求与范围更正。
- [x] 产品调查、源码证据及维护活跃度复核。
- [x] 官方图片和视频索引；用户确认 Control UI 显示与播放正常。

## P1 · 初版技术选择 — 已完成本轮

- [x] 选择 Three.js 轻量标注层；不依赖或改造 3D-Annotator。
- [x] 本地 Node 服务与 GLB／STL，保留原文件和材质。
- [x] 原 OpenClaw 会话桥接，`deliver:false`；独立网页左右布局。
- [x] 参数支架、人偶及真实 GLB／STL样例，版本与选区结构。

出口：有证据的候选比较和具体实施方案，不把接口存在当作已经接通。

## P2 · 最小原型 — 已完成开发者验证，待用户试用

- [x] 同一查看器显示两类样例模型。
- [x] 编号、可见面区域塗选、保存和恢复、撤销重做。
- [x] 一次反馈保存结构化三维选区；正确会话桥接协议与真实 Gateway 接纳探针。
- [x] 版本锁、并发保护、重试幂等、真实模型资源释放。
- [ ] Portal 语音确认后建立入口并验证真实交互。
- [x] 用户已在同台 Mac 的 Chrome 试用并提交两个点标签及红色区域；已开始反馈软件问题，不等于完整改模验收通过。

## 用户试用反馈 · 待修正（2026-09-09）

- [x] 画笔显示及回传改为实际笔迹覆盖范围，不再整三角面填色；保留可见面限制。
- [x] 数字／字母仅用于点标签；涂抹区域按颜色识别，避免称作不存在的「三号点」。
- [x] 合并观察与点标签操作：左键按住拖动旋转，表面双击落一个标签，单击不落点；不用在工具栏切换。验收拖动不误标、双击不重复标、不与缩放／聚焦冲突；用户已明确选定双击方案。
- [x] 移除审阅页左侧重复聊天记录与输入框，扩大模型空间；保留保存／提交状态。
- [ ] 标注只回传发起审阅的原会话，Agent 回复也回原会话；分别验证各入口，不宣称未来平台已兼容。
- [ ] 优先实现并实测 Control UI 侧边模型面板，保留原会话。导航例外已经生效，但用户实测旋转失败、画面闪烁；当前 Browser 是截图／单击转发，不是实时网页容器。已定位并重现，尚未修复完整交互。重新评估真正宿主视图／插件的接入，不把仅移除加载遮罩、另开 Chrome 或独立 Portal 当作完成。见 SIDEBAR-DIAGNOSIS-20260909.md。
- [ ] ChatGPT App 列为后续同类侧边交互兼容目标；核实宿主能力后再选接入方式，尚未实现。

用户已收齐意见并授权改进。上述已勾选项为代码及隔离验证完成；0.2 已于 15:36 单次更新本地服务并核对正式页，原模型／标记／锁保留。Control UI 导航配置后来已应用，真实侧边操作未通过；原 0.2 应用测试不能当宿主兼容验收。详见 ACCEPTANCE-V02-20260909.md 和 SIDEBAR-DIAGNOSIS-20260909.md。

出口：用户可实际操作，Agent 得到真正三维选区，而非只有截图或链接。

## P3 · 修改闭环与日常使用 — 待继续

- [x] 提供参数化源样例、重建及 Agent 发布 CLI。
- [ ] 用用户真实修改要求验收参数化修改闭环。
- [ ] 验证人偶实际可用的修改／重生成路径。
- [x] 初版版本校验、标注保护和异常恢复。
- [ ] 按实际使用反馈完善后台历史保留与回退策略。
- [ ] 用户验收后，再决定日常入口、部署和维护方式。
