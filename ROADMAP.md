# MeshCue · 阶段与待办

## 最新：整合安装与新会话入口计划（2026-09-10 18:33）

Kelven 要求「先做这一层的开发计划，想清楚再动手」。[OpenClaw 整合交付计划](INTEGRATION-DELIVERY-PLAN.md) 已整理为拟 0.5 阶段：完整插件内附 Skill、共用核心、项目／会话隔离、依赖与服务管理、干净安装和全新会话验收。**目前仅文档规划，未开始实现／发布 Skill、安装插件、修改配置或操作现有服务。**

0.4 的内网试用、长期授权、更名与 GitHub 接入已经完成；最新既有代码基线为 `8da32db`，实际 Windows 标记→真实 Telegram→改模再审仍待验收。下方「无远程／未推送／入口未交付」等按日期保留为历史状态，不从中恢复旧待办。当前 43176、正式旧数据及侧栏未提交改动继续保留。

> **当前恢复点（2026-09-10 14:18 开工后）**：0.4 候选已实现，分支 `feat/v0.4-lan-delivery`；正式0.3数据及服务未切换。参见 [验收与交付边界](ITERATION-V04-RESULTS.md)。下方旧暂停及侧栏阶段记录不再是当前待办。

## 0.4 内网交付与原会话闭环（候选）

- [x] 16:32采纳长期记住浏览器：30天闲置到期、真实使用续期、重启恢复、按浏览器撤销；旧60分钟要求被取代。当前开发与验收见[BROWSER-TRUST.md](BROWSER-TRUST.md)。

- [x] 固定实时0.3运行／用户数据基线，建立隔离环境；确认原服务未运行，不推断停机原因。
- [x] 原会话来源绑定与Telegram显式话题接线；隔离双话题回传、旧提交重试和活跃审阅保护。
- [x] 普通非localhost HTTP的UUID、SHA校验、标注、刷新及草稿接续兼容；不是Windows实机验收。
- [x] 受保护的内网监听、地址发现与短期授权核心；真实私网网卡短时隔离验证。
- [x] 隔离浏览器完整孔径改模／新版再审、授权失效草稿保护及同模型跨话题缓存隔离。
- [x] 普通链接的内网定向入场适配：核对客户端 IPv4 后由服务器直接发 HttpOnly 会话；不恢复配对、不通过聊天／URL投递凭据。隔离浏览器自动领取通过，Windows 端上仍待验证。
- [ ] 真实Windows入口、Telegram可见回执、理解回显及独立参数化修改／新版再审闭环。

候选独立开发已完成，完整用户交付仍待以上两项。配对建议已撤下，暂不增加标注工具、公网、内置浏览器或安装封装。GitHub 由 Kelven 在本轮后设置，本轮不建远程、不推送。原 [开发计划](ITERATION-V04-PLAN.md) 保留。

## 当前：标准浏览器交互基准，取消内置浏览器适配（2026-09-10）

Kelven 已明确决定：正式交互窗口只用标准浏览器，不适配 OpenClaw／Codex 等内置浏览器。保留跨 harness Agent 接线、原会话回传、共用核心与一体化扩充安装方向。权威范围见 [入口决策](BROWSER-ACCESS-DECISION.md)。

- [x] 更新定位、需求、验收及入口说明，取消侧栏／Portal 待办。
- [x] 标明旧宿主发布链停止，保留既有补丁与测试事实；不要求用户为该链重启 Gateway。
- [x] 按新范围独立启用 0.3，候选与实时数据保全核对通过；未发布暂存宿主 UI、未重复应用后端补丁、未重启 Gateway。
- [x] 正式 0.3 在独立 Chrome 只读加载成功，直接下载与当前模型 SHA 一致；没有抢锁或在正式数据上提交测试标记。见 [发布记录](STANDARD-BROWSER-V03-20260910.md)。
- [ ] 在标准浏览器完成核心新功能、原会话回传及当前版本原档下载验收；旧侧栏证据不冒充新入口或全部浏览器兼容性验收。
- [ ] 确立支持的浏览器／版本矩阵，优先沿用已有 Chrome 证据，再按计划验证其他浏览器；保持普通及并排窄窗口的可用布局。

蓝图变更时只修改了文档；随后按用户要求提供最新版本，已独立启用工作台 0.3。宿主补丁／配置不变，安装包仍不提前开发；如后续需要清理历史宿主改动，单独核对与安排，不自动执行旧维护脚本。

## 历史：0.3 侧栏候选完成时的发布安排（2026-09-09，已被取代）

Kelven 在本轮设计点累积完成后明确提出「可以先做一版迭代」，现在开始实现 R04、R18–R29 的已确认要求。安装包／跨 harness 封装仍不在本轮范围；不修改用户的模型、旧提交或审阅锁，不代用户重启 Gateway。

实现与验收证据见 [0.3 迭代记录](ITERATION-V03.md)。核心候选保留；原来以 Gateway 重启和宿主 UI 发布为前提的安排已取消，不再从这里恢复。

## 历史：先集中讨论，再成批开发（2026-09-09）

Kelven 明确要求继续讨论多个产品改进点，不因某一项设计确认就立即修改程序；待本轮改进点收齐，整理范围、优先级与验收要求后，再集中进行开发。当前仅维护设计与待办，不启动逐项实作、安装包开发或服务变更。

- **已确认细节**：Agent理解回显采用独立可隐藏高亮，不覆盖原标注／模型；保持视角，用户点选才定位；用户在原会话更正偏差，Agent只修订自己的高亮，无需重新标注。见需求R18。
- **已确认的点标签规则**：仅用大写字母；同轮 A/B/C/D 删除 B 后留 A/C/D，新增为 E，不重排或复用。见需求R04。仅设计确认，程序和旧数字标记未改；超过 Z、跨轮起算等细节待讨论。
- **方向认可、细节待讨论**：标注单项管理的其余细节；提交异常／重试与后续处理状态的呈现。
- **已确认的全局隐藏**：同一按钮切换隐藏／显示全部用户点标签、笔迹及 Agent 理解高亮，便于预览干净模型；不是删除或取消提交。见需求R19。隐藏期间的标注操作及单层可见状态恢复细节待讨论，未开始实现。
- **已确认的撤销删除**：误删后恢复同一个原标记及其字母／位置，删 B 再撤销仍是 B，不当成新点或重排其他标签。见需求R20。基础撤销已有实现，新字母规则须集中核验，本次未改程序。
- **已确认的标注清单**：可展开／收合；点选 C 只高亮 C，不转视角，另点「定位」才移动视角；收合后留出大模型画面。见需求R21。布局、笔迹条目与全局隐藏时的交互细节另议，当前未改程序。
- **已确认的标签重新定位**：清单内发起后点击新位置，保留同一个标签及原字母，不新增常驻工具；旧提交不变，新位置留草稿等待再次提交。见 R26。须与只转视角的查看定位区分，未改程序。
- **用户明确新增：紧凑工作区**：压缩顶部标题、上下留白与非必要纵向占位，尽量扩大实际模型预览窗口。见 R27，须在实际侧栏尺寸验收，未改布局。
- **用户明确新增：下载当前版本**：直接取得当前实际预览的模型，无需再要求 Agent 整理文件；不可误下后台新版。见 R28，须验证实际宿主文件交付，未实现。
- **已采纳的彩色模型呈现**：原色／贴图为主要视图，标注用图案与描边区分，不只依赖颜色；自选素色检视作辅助，原材质与下载文件不变。见 R29。图案密度、线型与入口等细节待试用，未改程序。
- **已确认的提交回馈**：分开显示「已保存」「已送到原会话」「Agent 已读取」，各自须有对应真实回执；已读不代表已改模。见需求R22。当前只更新设计，未修改提交机制或界面。
- **已确认的提交后编辑**：原提交批次保持不变，之后新增／修改留草稿并显示「尚未提交」，用户再次提交才送出，不自动发送；旧回执不代表新改动已提交。见需求R23。跨批表达及快照／增量细节待设计，本次未改程序。
- **已确认的橡皮擦与油漆桶方向**：画笔可切换为局部擦除标注；油漆桶跨相邻且方向接近的三角面填色，同时限制相对固定起始面的方向差，避免沿曲面越填越远，不是只填单个三角面。见 R24／R25。两项角度阈值、遮挡边界及重叠颜色擦除细节待议，未开发。
- **已确认的油漆桶预览**：油漆桶模式下悬停预览范围，单击正式填色，无额外确认弹窗；预览不改草稿，填色不自动提交。见 R25 交互补充，当前未实现。
- **纳入试用、可用性待验**：油漆桶范围滑杆，收窄／放宽并即时预览，不手输角度。用户担心工具过多使界面复杂，最终留待实测评估；布局拟用上下文显示减少常驻控件，具体方案未定。此确认不启动开发，仍先整理本轮设计。
- **继续收集**：其余用户意见、高亮样式、多范围对应、回显状态及异常流程。讨论中的建议与已确认要求分开，未确认细节不自动定案。

下方包含早期实施阶段记录，不能据其中旧授权自动提前启动本轮新增优化。

项目入口：[PROJECT.md](PROJECT.md)。2026-09-09 用户已授权多轮迭代完成可测试初版；当前完成范围见 [验收记录](ACCEPTANCE-20260909.md)。

## P0 · 立项整理 — 已完成

- [x] 正式项目目录、主文件和记忆索引。
- [x] 项目级本地 Git 版本管理及初始文档快照；未配置远程仓库。
- [x] 统一审阅界面的需求与范围更正。
- [x] 产品调查、源码证据及维护活跃度复核。
- [x] 官方图片和视频索引；用户确认 Control UI 显示与播放正常。

## P1 · 初版技术选择 — 已完成本轮

- [x] 选择 Three.js 轻量标注层；不依赖或改造 3D-Annotator。
- [x] 本地 Node 服务与 GLB／STL，保留原文件和材质。
- [x] 原 OpenClaw 会话桥接，`deliver:false`；独立网页左右布局。
- [x] 参数支架、人偶及真实 GLB／STL样例，版本与选区结构。

出口：有证据的候选比较和具体实施方案，不把接口存在当作已经接通。

## P2 · 最小原型 — 已完成开发者验证，待用户试用

- [x] 同一查看器显示两类样例模型。
- [x] 编号、可见面区域塗选、保存和恢复、撤销重做。
- [x] 一次反馈保存结构化三维选区；正确会话桥接协议与真实 Gateway 接纳探针。
- [x] 版本锁、并发保护、重试幂等、真实模型资源释放。
- **已取消（2026-09-10）**：Portal 入口建立与交互验收；改用标准浏览器。
- [x] 用户已在同台 Mac 的 Chrome 试用并提交两个点标签及红色区域；已开始反馈软件问题，不等于完整改模验收通过。

## 用户试用反馈 · 待修正（2026-09-09）

- [x] 画笔显示及回传改为实际笔迹覆盖范围，不再整三角面填色；保留可见面限制。
- [x] 数字／字母仅用于点标签；涂抹区域按颜色识别，避免称作不存在的「三号点」。
- [x] 合并观察与点标签操作：左键按住拖动旋转，表面双击落一个标签，单击不落点；不用在工具栏切换。验收拖动不误标、双击不重复标、不与缩放／聚焦冲突；用户已明确选定双击方案。
- [x] 移除审阅页左侧重复聊天记录与输入框，扩大模型空间；保留保存／提交状态。
- [ ] 标注只回传发起审阅的原会话，Agent 回复也回原会话；分别验证各入口，不宣称未来平台已兼容。
- **已取消（2026-09-10）**：继续维护 Control UI 侧边模型面板；之前的开发及阶段验收保留为历史，见 SIDEBAR-DIAGNOSIS-20260909.md 与 SIDEBAR-ACCEPTANCE-20260909.md。标准浏览器现在就是正式目标入口。
- **已取消（2026-09-10）**：Codex／ChatGPT App 等内置浏览器与侧边交互兼容；其 Agent 工具接线方向仍保留，另行核实。

用户已收齐意见并授权改进。上述已勾选项为代码及隔离验证完成；0.2 已于 15:36 单次更新本地服务并核对正式页，原模型／标记／锁保留。Control UI 导航配置后来已应用，真实侧边操作未通过；原 0.2 应用测试不能当宿主兼容验收。详见 ACCEPTANCE-V02-20260909.md 和 SIDEBAR-DIAGNOSIS-20260909.md。

出口：用户可实际操作，Agent 得到真正三维选区，而非只有截图或链接。

## P3 · 修改闭环与日常使用 — 待继续

- [x] 提供参数化源样例、重建及 Agent 发布 CLI。
- [ ] 用用户真实修改要求验收参数化修改闭环。
- [ ] 验证人偶实际可用的修改／重生成路径。
- [x] 初版版本校验、标注保护和异常恢复。
- [ ] 按实际使用反馈完善后台历史保留与回退策略。
- [ ] 用户验收后，再决定日常入口、部署和维护方式。
