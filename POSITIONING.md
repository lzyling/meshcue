# 产品定位补充：Agent 通用 3D 审阅与标注工具

确认日期：2026-09-09。Kelven 确认通用工具定位，要求可适配不同 Agent harness；本次补充定位文件，不启动新的接口开发或平台部署。

**2026-09-10 更新：交互入口统一为用户自己的标准浏览器，不再适配 OpenClaw、Codex 等宿主内置浏览器／侧栏。跨 harness 的 Agent 接线仍保留。** 详见 [入口决策](BROWSER-ACCESS-DECISION.md)；本次仅改蓝图，不修改运行中服务或既有补丁。

## 一句话定位

**一个在标准浏览器中使用、面向不同 Agent harness 的通用 3D 审阅与标注工具：让用户直接在模型上指出修改位置，再由原会话中的 Agent 理解要求并调用建模工具。OpenClaw 是首个 Agent 适配平台，而不是产品的唯一归属。**

这里的 harness 指承载 Agent 的运行、工具调用及会话交互的应用环境，不是大语言模型本身。接入另一个模型服务，不等于已适配另一个 harness。

## 交付体验补充：从原对话取得可用网址（2026-09-10）

Kelven 以当前 Telegram 对话为例明确：Agent 在原对话给出网站地址，用户能够顺利打开并预览模型，不必进入 Control UI，也不以适配其界面为前提。Telegram 是对话入口，Agent harness 是运行／接线平台，独立网页是模型交互入口，三者不混为一层。

目标流程为「Telegram 提需求 → 原话题收到预览链接 → 网页预览／标注 → 原话题继续沟通」。链接需对用户实际使用的设备／网络可达，并加载正确模型；不能把仅在服务主机可用的回环地址当作通用交付。09-10 13:46 已明确增加 Windows 通过内网访问 MacBook Pro 工作台；公网／外出访问未确认。用户要求短期访问授权、每次重新发放更换，避免其他内网设备未经授权改动标注；不把它与长期 API 密钥混用。13:54 用户再次明确收到链接直接进入、愿意接受短期授权的有限风险，不增加配对／确认；此前配对建议已撤下。用户体验要求已定，当前环境禁止聊天／URL传递有效访问凭证这一适配限制尚未解决，不把它改写成产品的额外用户步骤或声称已经可交付。现有原会话回传要求保留，本轮没有改监听、迁移运行绑定或验收 Telegram 闭环。详见 [入口决策](BROWSER-ACCESS-DECISION.md)。

## 解决什么问题

工具负责把「改哪里」表达清楚：共同查看模型、表面点标签、彩色笔迹、保存与提交真实三维位置，并标明所属模型版本。用户在发起审阅的原会话说明「怎样改」，Agent 负责理解、执行建模操作并交付下一版。

参数化零件与 Tripo 等来源的人偶使用同一套审阅流程；保留原始模型及可编辑工程，不把建模方式混同于审阅入口。

## 软件、Skill 与 MCP 的关系

| 层次 | 职责 | 当前状态 |
| --- | --- | --- |
| 核心软件 | 模型显示、点标签、可见面笔迹、保存、提交数据、版本及草稿保护 | 已有独立运行的本地网页与 Node 服务 |
| Harness 适配 | 交付模型、绑定原会话、回传标注、发布新版本及提供标准浏览器入口 | 当前有 OpenClaw 自订桥接；通用适配层尚未完成抽离 |
| MCP 接口 | 以标准工具协议供支持它的 harness 调用工作台能力 | 未实现；是未来可选接入方式，不是产品本体 |
| Skill／操作指引 | 告诉 Agent 如何正确使用工具、理解标注与遵守版本保护 | 有项目操作文件，未封装为正式 Skill |
| 标准浏览器交互 | 用户自己的浏览器直接渲染、接收输入及下载文件 | Chrome 有实际使用与测试证据；其他浏览器／版本需按支持矩阵核验，宿主内置浏览器适配已取消 |

上述层次可以同时存在，不是三选一。Skill 不替代工作台程序，MCP 也不自动提供侧边界面、会话通知或建模能力。

## 当前实现与目标边界

当前实物是 **本地网页工作台 + OpenClaw 自订接线**，现有环境另有历史 Control UI 浏览器补丁。已有模型预览、标注及保存能力，但完整 Agent 回传链路仍依赖已配置的 OpenClaw 环境；不能称为已完全解耦、任何 harness 即插即用的成品。

面向未来，模型、标注、草稿与版本数据应归工作台管理；平台差异放在适配部分，包括如何交付文件、识别原会话、通知 Agent 和提供标准浏览器入口。这是已确认的设计方向，不表示这些模块已全部实现。

为修复右侧操作而修改的 Control UI 浏览器属于历史宿主兼容工程。新设计取消该维护路线，不将截图、输入或下载转发补丁带入正式交付，也不要求工作台升级伴随 Gateway 重启。既有补丁本次不撤销，0.3 旧侧栏发布链按入口决策暂停；独立工作台的发布路径需另行核对。

## 跨平台仍要保持的原则

- 原会话是唯一对话入口。工作台不复制聊天框，不把标注投递到无关会话或渠道。
- 标注包含不可变模型身份、版本、网格定位、三维位置／笔迹范围及必要视角；截图只作辅助。适配时不得降级成只有截图而丢失位置语义。
- 编号属于点标签，涂抹区按颜色与范围区分；新笔迹不扩大成整块三角面。
- 提交是位置反馈，不是自动改模指令。说明不足时 Agent 回原会话询问；明确是功能测试时不据此修改模型。
- 开始审阅前由 Agent 交付正确版本；用户标注中不得强制换模。提交不自动解锁，草稿及版本保护不因换平台而失效。
- 「工具调用接通」「标注回到正确会话」「标准浏览器内完整操作与文件下载」分别验收。标准浏览器窗口／标签页就是正式交互入口，不再要求侧边嵌入；仅提供链接仍不等于完整功能与回传闭环通过。

## 非目标与尚未承诺

不是新的聊天客户端、独立 Agent、大语言模型或完整 CAD／雕刻软件；不取代各 harness 的主 Agent，也不承诺任意网格能自动精修或直接打印。

多 harness 的 Agent 接线是正式适配方向，不是已完成清单。OpenClaw 现为首个实现；Codex、ChatGPT App 等未来接线须核实平台接口，不再研究其内置浏览器或侧边展示兼容。MCP、正式 Skill、其他平台适配、分发渠道、安装技术及商业化方式尚未定案；面向用户的一体化安装原则已按下节确认。

本地浏览、添加三个点标签及回传原会话已有用户阶段验收。新画笔用户手感、Agent 实际改模／新版再审闭环不能由该结果一并判为通过。

## 交付原则：面向非开发用户，一步到位安装

2026-09-09 Kelven 明确选择一体化安装，并排除把「进阶用户自行组装」作为正式产品选项。设计不假设建模用户具有开发经验，无需以建模与编程人群重合程度作为这一要求的依据。

- 用户面对一个完整安装流程，必要时选择受支持的 harness 并完成其要求的授权，随后即可打开审阅入口；不是先装程序、再寻找并手动装 Skill／配置 MCP。
- 安装流程应统一处理工作台运行依赖、适配组件及所需操作指引的配置，并检查宿主版本和连接状态。用户不必运行开发命令、编辑配置文件或理解内部模块。
- 成功标准不是「文件安装完成」，而是支持的环境中可打开模型、标注，并将反馈送回正确的原会话；不兼容时给出清晰可执行的处理提示，不把源码补丁／手动组装当作交付。
- 核心程序与不同 harness 的适配仍保持架构分责，但不把内部拆分变成用户必须完成的安装步骤。开发者源码与维护说明不等于第二套面向用户的安装模式。
- 「一步到位」指统一、省去技术组装的体验，不是承诺字面上只有一次点击或跳过系统／宿主授权。安装包形式、支持的操作系统和版本范围后续另定。

当前尚未实现上述安装交付；现有 README 的开发／本机启动命令不是正式产品用户的最终安装流程。本次仅确立产品要求，不开始打包、安装或修改服务。

### 已确认的后续设计基准：安装一个扩充即可使用

Kelven 随后明确同意按这个定位推进后续设计：用户体验像「安装一个 Skill」一样简单，实际交付可以是一个完整扩充套件，而不必局限为一份 Skill 文件。

- 共用同一套工作台核心，分别提供适合 OpenClaw、Codex 等受支持 harness 的包装与接驳，不分叉为不同核心产品。
- 用户只面对一次扩充安装流程；工作台、依赖、平台接线和所需操作指引由套件统一处理。使用时在原会话请求「打开模型审阅」，通过标准浏览器访问，不要求手动启动或管理另一个程序；窗口独立不等于安装要手动组装。
- Skill、MCP、插件是内部实现或分发机制，不作为用户必须了解、选择和分别配置的技术选项。具体采用哪一种由各平台能力决定。
- 安装后可用是目标，不是已实现承诺。实际安装、运行依赖、权限、平台工具接口兼容、原会话回传及标准浏览器交互均须验收；不包含宿主侧边交互，不能由某平台支持插件推断整套工作台已可直接安装。
- 现阶段继续讨论产品与交付设计；到安装交付阶段再落实套件实现，不提前开发安装包、注册 MCP 或发布 Skill。

## 名称与管理

对外描述采用「Agent 通用 3D 审阅与标注工具」；现有项目代号及目录 `3d-agent-review` 保留。「OpenClaw 3D 模型审阅工作台」是早期项目名和首个适配实例的称呼，不再代表仅供 OpenClaw 使用。

本文件补充并优先解释旧文档中的 OpenClaw 专属措辞；既有交互要求和数据保护规则不变。此轮只写定位，不新增 MCP、Skill、平台安装或服务变更。
